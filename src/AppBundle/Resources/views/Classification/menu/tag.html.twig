<div class="page-header">
    <h3>Tags</h3>
</div>

<div class="cw-tags">
    {% for object in objects %}
        <a href="{{ url('sonata_news_tag', { 'tag': object.slug } ) }}">{{ object.name }}</a>
    {% endfor %}
</div>
